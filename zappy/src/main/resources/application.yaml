spring:
  application:
    name: zappy
  datasource:
    url: jdbc:postgresql://localhost:9000/zappytest
    username: testUser
    password: password
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: create
        jdbc:
          time_zone: Asia/Kolkata
server:
  port: 8081

